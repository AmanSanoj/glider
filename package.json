{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "nuxt-icon": "^0.1.8",
    "sass": "^1.56.2"
  },
  "dependencies": {
    "@nuxtjs/color-mode": "^3.2.0",
    "firebase": "^9.15.0"
  }
}
